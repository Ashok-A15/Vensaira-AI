{
  "buildCommand": "cd vensaira-react && npm install && npm run build",
  "outputDirectory": "vensaira-react/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
